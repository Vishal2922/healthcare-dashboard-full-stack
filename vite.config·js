import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    // Proxy API calls to PHP backend during development
    // This avoids CORS issues when running locally
    proxy: {
      '/api': {
        target: 'http://localhost/clinic_backend/public',
        changeOrigin: true,
        rewrite: (path) => path,
      },
    },
  },
});